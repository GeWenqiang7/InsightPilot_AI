# Model Skill Execution Policy

`model_skill` should:

- consume FE and EDA context before suggesting model strategies
- keep candidate model families structured and task-aware
- expose evaluation priorities in a stable format for downstream agents
