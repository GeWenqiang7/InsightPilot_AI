# Evaluate Skill

`evaluate_skill` consumes `model_result` (and optional `eda_result`) to produce:

- risk-focused evaluation summary
- metric review protocol
- prioritized improvement actions
- reviewer gate decision (`proceed` / `needs_review`)
- compact `evaluate_for_llm`
